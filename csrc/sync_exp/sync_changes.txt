1.  added support for delta names response in Root Advise interests

new DTAG needed: CCN_DTAG_SyncNodeDeltas (127)

new version number in CCN_DTAG_SyncNodeDeltas: SYNC_UPDATE_VERSION (20120307)

new env option: CCNS_DELTAS_LIMIT (0-8000, default 0, use 4000 to turn on)

default is off, although even when off the processing will be OK if it sees  a response with CCN_DTAG_SyncNodeDeltas

2. removed use of CCN_API_VERSION

obsolete

3. use topoPrefix instead of localhost to start local node names

allows repo to serve up nodes

4. support scoped and unscoped sync interests

new env option: CCNS_SYNC_SCOPE (0-2, default 2, use 0 for unscoped)

does it make sense to allow scope = 1?